# 📣 X 评论机器人屏蔽器 — 宣传物料文案(可直接复制)

> 用途:生成 2-3 张宣传图片的文案。每张图的内容是独立区块,整块选中复制即可。
> 建议:AI 生图工具对中文渲染差,文字层请用 Canva / Figma / 稿定设计 叠加。

---

## 🖼 图 1:主视觉(Hero 海报)

主标题:

评论区里的垃圾账号,一键扫干净。

副标题:

选取一条 bot 评论当模板,整片评论区的"约炮引流号、刷单诈骗号、色情营销号"自动现形、自动屏蔽。

卖点标签:

🎯 三维匹配:用户名 · 文本相似 · 评论关键词,任一命中即处理
🧬 模板驱动:点一条 bot 评论,它就成了"通缉令",全网变体都认
🛡 防误伤闭环:仅标记试运行 / 白名单 / 阈值可调 / 屏蔽记录可追溯解封
💾 数据主权:全量导出 JSON,数据不锁定在浏览器

底部条:

Tampermonkey 油猴脚本 · 纯本地运行 · 内置 91 条高频规则 · MIT 开源免费 · v0.14.3

---

## 🖼 图 2:核心能力(功能详解卡)

标题:

它不只是"按关键词屏蔽"——它理解 bot 的套路。

六大能力:

1. 三维匹配矩阵 —— 同一名单管用户名、模板相似度抓新句式、评论关键词兜底新变体,互不干扰不误伤
2. 高频特征词挖掘 —— 已屏蔽的垃圾账号自动提炼共现特征,词频条形图,一键入名单
3. UI 自动化屏蔽 —— 模拟真人点击路径(菜单→Block→确认→验证),不碰内部 API,屏蔽成功才计数
4. 防风控设计 —— 屏蔽间隔可调(默认 1.2s)、轮数上限、闲置自动停止、结果验证
5. 屏蔽记录(blocklog) —— 每次屏蔽留痕:谁、什么时候、为什么;误伤一键跳主页解封、加白名单防重锁
6. 数据全量导出 —— 下载 JSON / blocklog.json / 文本 / 日志;Gist 同步跨设备,规则迭代不动代码

差异点角标:

大多数同类脚本按关键词/粉丝数过滤,它是"模板驱动 + 三维匹配"。

---

## 🖼 图 3:诚实页(局限与使用建议)

标题:

它很能打,但不是魔法。先看这些。

已知局限:

⚠️ 文本相似 ≠ 语义:批量屏蔽存在误伤可能,内置阈值 0.75 可调
⚠️ 名单里的宽泛词(如"免费""学生")可能误伤正常账号 → 及时清理 + 白名单
⚠️ 部分自动生成用户名(拼音+数字)的真实用户可能被内置正则误伤(已知问题,迭代中)
⚠️ 依赖 X 页面结构,前端改版可能导致选择器失效(有三重 fallback 兜底)
⚠️ 自动屏蔽可能违反 X 服务条款,账号风险由使用者自行承担

正确打开方式:

1. 先跑"仅标记"试运行,确认命中质量再开自动屏蔽
2. 屏蔽间隔 ≥1000ms,每天 ≤100 个,防风控
3. 误伤了?屏蔽记录里找到它 → 主页解封 → 加白名单

底部条:

⚠️ 仅供个人研究使用 · 开源地址 github.com/kikuxdev/X-Comment-Bot-Blocker
