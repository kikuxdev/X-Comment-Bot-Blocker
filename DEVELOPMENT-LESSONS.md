# 开发复盘:X 评论机器人屏蔽器

> 本文档记录本项目从 0.1.0 到 0.11.4 迭代过程中踩过的坑与教训,供后续油猴脚本开发复用。
> 精简版已沉淀为全局 skill:`~/.pi/agent/skills/userscript-dev/SKILL.md`(任何会话自动可用)。

## 一、过程侧教训(需求与协作)

| # | 教训 | 本项目案例 | 以后怎么做 |
|---|---|---|---|
| 1 | **需求不要演进式开发** | 从"评论相似度屏蔽"起步,陆续加了用户名维度、导出、内置规则、词频、Tab、评论关键词……每个新需求都要动 UI 和存储 | 动手前先列完整功能清单 + 数据模型设计,一次定型;新增功能走"设计稿 → 确认 → 实现" |
| 2 | **先设计数据模型** | 存储键从 1 个长到 4 个;名单条目从字符串迁移到 `{d,h}` 对象 | 先画 JSON schema(键、字段、类型、版本号),预留迁移函数 |
| 3 | **规则数据与代码分离** | 91 条内置规则一度硬编码,每次更新规则都要改脚本 | 规则走外部 JSON/Gist,代码只留基线兜底;用户迭代规则 ≠ 发布代码 |
| 4 | **实测优先于单测** | 单测全绿,实弹一跑:handle 大小写、确认按钮、风控失败,三个大坑全是 DOM 环境问题 | 纯逻辑单测 + DOM 交互必实弹;给用户"仅标记"试运行模式 |
| 5 | **批量操作要预览** | 高频词"全部加入"把噪声 bigram(快速/无忧/线下)灌进名单,误伤正常用户 | 批量导入/加载前展示清单;规则分风险等级(低/中) |

## 二、技术侧教训(按踩坑时间线)

### 1. 正则大小写陷阱(最隐蔽)
- **现象**:handle 正则(`[A-Z][a-z]+\d{4,}`)实测命中率为 0。
- **根因**:X DOM 中的 @handle 一律小写;单测用了大写样本,测试盲区。
- **修复**:正则小写兼容 + `i` 标志;测试固定用小写输入。
- **通用规则**:面向外部 DOM 写正则前,先确认数据实际形态。

### 2. 确认按钮可能点到 Cancel
- **现象**:统计的封禁数 > 实际封禁数。
- **根因**:通用选择器 `[role=group] [role=button]` 取到第一个按钮(可能是取消)。
- **修复**:优先 `data-testid="confirmationSheetConfirm"`,回退取按钮组最后一个。

### 3. 屏蔽后不验证
- 点击确认直接计成功,风控失败也算数。
- **修复**:验证推文从 DOM 移除(`!article.isConnected`),未移除记 `unverified`,不计入统计。

### 4. 事件冒泡撤销状态(最小化"没效果")
- 按钮点击先收起面板,click 冒泡到面板的"点击恢复"处理器又立刻展开。
- **修复**:恢复处理器排除按钮自身(`!e.target.closest('#toggle')`)。
- **通用规则**:容器级 toggle 处理器必须排除自己的控制按钮。

### 5. 残留旧守卫短路新逻辑
- 加用户名维度时,旧的 `if (bestIdx < 0 || best < threshold) return;` 留在前面,用户名命中永远走不到。
- **修复**:维度独立判定,每个维度自己的守卫;删代码时检查旧守卫是否失效。
- **教训**:重构加功能时,逐行检查旧 early-return。

### 6. 统计口径混淆
- 本次增量 vs 历史累计:用户拿"本次封禁 21"对照 X 屏蔽列表 100+,必然对不上。
- **修复**:新增持久化累计统计(`xcbb_stats`),口径写进 tooltip。

### 7. 测试断言错误(3 次)
- 期望 0.3 实际 0.000(脚本表现更好);期望单字 token(设计只产 bigram);模板归一化不一致(去重正确)。
- **教训**:测试失败先质疑测试,再质疑代码;修正期望后重跑,而不是改代码。

### 8. 工具使用(编辑类)
- 漏传 `path` 参数、跨文件编辑放同一调用、锚点不匹配导致整批原子失败。
- **教训**:一次编辑调用只针对一个文件;改大段前先 read 确认锚点。

### 9. 内存滞留
- `seen` 集合持有已脱离 DOM 的评论节点直到下次扫描。
- **修复**:扫描结束时 `clear()`;日志 300 行上限、语料 500 条上限、Blob URL 延迟回收。

### 10. 文档落后于代码
- README 多次落后于实际功能,发布前需要核对版本号与特性。
- **教训**:版本号 + README 每次发布同步更新;可用脚本自动核对(BUILTIN 计数等)。

## 三、油猴脚本开发 Checklist(开新项目直接抄)

**设计期**
- [ ] 功能清单定稿,不边写边加
- [ ] JSON schema:设置/名单/语料/统计,带 version 字段 + 迁移函数
- [ ] 规则数据外置,代码只留基线

**编码期**
- [ ] DOM 选择器写成 fallback 链(testid → 结构特征 → 文本)
- [ ] 正则面向真实数据形态(小写、emoji、零宽字符)
- [ ] 容器级点击处理器排除自身控制按钮
- [ ] 破坏性操作(屏蔽/删除)后做 DOM 验证
- [ ] 统计区分"本次"与"累计",口径可视化
- [ ] 内存:集合用后即清、日志/语料设上限、Blob URL 延迟回收

**测试期**
- [ ] 纯逻辑提取到 node 可跑的单测(含大小写/边界断言)
- [ ] 失败先检查断言,再检查代码
- [ ] 真实页面实弹验证(登录态),提供"仅标记"模式
- [ ] 程序化审计:所有 `el('id')` 引用 vs HTML 模板交叉验证

**发布期**
- [ ] @version 递增 + @updateURL/@downloadURL 指向 raw 地址
- [ ] README 与代码同步(版本号、特性表、截图)
- [ ] 脏数据/私密数据不进公开仓库

## 四、可复用代码模式(本项目已验证)

```js
// 1. 存储迁移兼容
let badnames = (GM_getValue('xcbb_badnames', []) || []).map((e) =>
  typeof e === 'string' ? { d: e, h: null } : e);

// 2. 会话级去重(按稳定 ID, 而非 DOM 节点)
const dedupeKey = `${handle.toLowerCase()}/${tweetId}`;
if (tweetId && processed.has(dedupeKey)) return;
if (tweetId) processed.add(dedupeKey);

// 3. 破坏性操作验证
const removed = await waitFor(() => !article.isConnected, 2000);
if (!removed) return 'unverified'; // 不计入统计

// 4. 容器 toggle 排除自身按钮
if (panel.classList.contains('collapsed') && !e.target.closest('#toggle-btn')) {
  restore();
}

// 5. 正则兼容真实 DOM 大小写
nameMatches: new RegExp(...).test(f) || new RegExp(...).test(f.toLowerCase());
```

## 五、一句话总结

**油猴脚本的开发分水岭在"外部 DOM 的真实性"**:单测保证逻辑不烂,DOM 兼容靠 fallback 链和实弹验证;数据模型和统计口径要一次想清楚,因为改它们的成本最高。
