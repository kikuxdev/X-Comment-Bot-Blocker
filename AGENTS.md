# AGENTS.md — X 评论机器人屏蔽器 开发指南

单文件 Tampermonkey 用户脚本(`x-comment-bot-blocker.user.js`,~1900 行 IIFE),在 x.com 评论区分三维识别并屏蔽 bot 账号。**所有逻辑都在这一个文件里**,按注释分节;数据走 GM 存储(键名见文件顶部「配置与持久化」)。版本当前 **0.14.x**。

## 上手顺序

1. 先读 `README.md`(用户视角功能全貌、数据导出路径)与本文件;
2. **改 DOM / 存储 / 统计逻辑前**,读 `DEVELOPMENT-LESSONS.md`(本项目踩坑史:handle 大小写、确认按钮、事件冒泡、统计口径、旧守卫短路等,node 测试测不出的坑全在那里);
3. 打开 user.js 按节浏览:配置与持久化 → 文本归一化与相似度 → DOM 解析 → 屏蔽 → 扫描 → 面板 UI → 事件绑定。

## 工作流(每次改动必须走完)

1. **改**:一个逻辑改动一个提交;匹配现有风格(中文注释、节标题 `/* ==== ... ==== */`);大段编辑前先 read 确认锚点。
2. **测**:
   - 纯逻辑:`node --check x-comment-bot-blocker.user.js && node tests/comment-kw.test.mjs && node tests/blocklog.test.mjs`,必须全绿;
   - DOM 行为:node 测不到,交给用户在浏览器里用「仅标记」模式实弹验证;
   - 测试失败先质疑断言/沙箱(提取函数引用了脚本作用域变量时要补桩,如 `currentTab`),再质疑代码。
3. **提交**:本地提交,消息写清 改了什么 / 为什么 / 怎么验证的。
4. **版本号单独批次**:升 `@version` 元数据 + `VER` 兜底常量 + README 主界面示例版本,单独一个 commit,不与功能混在一起。
5. 确认后 `git push origin main`(仓库保持 public,勿改 private)。

## 隐私红线(违反即事故)

- 用户数据(语料/名单/屏蔽记录/本地导出文件)**只允许读取做诊断,不得写入仓库、提交、issue 或文档**;
- `spam-rules*.json`、`*.swp` 已在 .gitignore,**不得 `git add -f` 强推入库**;
- 脚本默认 `remoteUrl` 必须保持空字符串:**任何用户的 gist URL 不得写进 DEFAULTS**(曾发生 secret gist URL 随旧版本泄漏、全历史清理的前科,勿重蹈);
- 新增导出功能默认只写用户本地文件;任何「数据入库」需求先问自己:这是公开安全规则还是用户数据?

## 架构要点

- **三维匹配**:① 用户名(名单,条目支持 `/正则/`)② 模板相似度(≥阈值)③ 评论关键词(**独立列表**,与名单无关),任一命中即处理;优先级 白名单 > 推文作者 > 规则。
- **相似度**:归一化(去 URL/@/#/emoji/标点/零宽)后取 bigram Jaccard / Levenshtein / 包含匹配 三者最高;短于 4 字符不参与文本判定。
- **屏蔽走 UI 自动化**:…菜单 → Block(图标 path / data-testid / 文本 三重 fallback)→ 确认弹窗 → 验证推文从 DOM 移除;未验证记 `unverified` 不计入统计;不调用内部 API。
- **统计口径**:本次(statScanned/Matched/Blocked,每轮清零)与累计(totals,持久化)严格分开。
- **隐藏层必须放行点击**:任何 `opacity:0` 后仍留在 DOM 的覆盖层(通知条等)要 `pointer-events:none`,否则会吃掉面板按钮的点击(0.14.1 事故)。
- **禁用按钮**用 `.xcbb-disabled` 类而非原生 `disabled`(保留点击事件以提示原因),视觉等价。

## 已知未做(缺口清单)

| 缺口 | 现状 / 线索 |
|---|---|
| 一键自动解封 | 方案已设计:资料页被动增强,复用 `findUnblockMenuItem()` / `findConfirmButton()`,解封后提示加白名单防重锁;blocklog 的 🏠 手动解封已就位 |
| 内置 handle 正则误伤 | `[a-z]{6,}\d{5,}` 会命中真实用户的拼音/英文名+数字手柄;收紧方案未定(可用语料校准,但语料是用户数据,只在本机分析) |
| 名单宽泛词污染 | 挖掘词(免费/学生/快速/高中…)进名单 → 用户名维度误伤;需「宽泛词清理」策略或条目级风险分级 |
| 深色主题 | 0.13.0 换浅色护眼后仅一套;CSS 已全部变量化,补第二套成本低 |
| 日志刷新即清 | 已知限制;导出需先点 [导出] |
| 解封后从语料移除 | 未做 |
| 与 X 官方屏蔽列表双向同步 | 无官方 API,未做 |
| 多语言 UI | 仅中文 |
| 评论关键词条目级管理 | 独立列表已有,但无单独 Tab 管理(现为选项页文本域) |
| GreasyFork 发布 | 文档 `GREASYFORK.md` 已备,尚未发布 |

## 已知优劣(Pros & Cons)

**Pros**

- 三维匹配 + 完整防误伤路径(仅标记试运行 / 白名单 / 阈值 / 逐个确认 / 屏蔽记录可追溯解封);
- 数据三层分离(内置公开规则 / 用户 Gist / GM 存储)+ 全量导出,规则迭代不动代码;
- UI 自动化屏蔽不碰内部 API,选择器三重 fallback,屏蔽结果验证后才计统计;
- 纯本地运行、无外部依赖、护眼浅色主题 CSS 变量化。

**Cons**

- 名单宽泛词 + 内置 handle 正则导致用户名维度误伤(当前最大的已知问题,见缺口清单);
- 单文件 ~1900 行,大段编辑锚点易错(编辑前先 read);
- DOM 行为依赖真机验证,node 测试覆盖不到;
- 评论关键词列表需手动维护,默认只含内置低风险词(63 条中排除 9 个日常常见词);
- secret gist 每次编辑内容后 URL 变化需手动更新;public gist 稳定但内容必须公开安全;
- 界面仅中文;无深色主题。

## 测试文件说明

- `tests/comment-kw.test.mjs`:评论关键词匹配(误伤回归 4 例 / 自定义列表 / 正则条目 / 常见词排除);
- `tests/blocklog.test.mjs`:屏蔽记录(按 handle 去重 / 上限 300 / 远程合并按最新时间戳 / 导出字段);
- 两者都用括号计数从 `user.js` **提取真实函数**执行(保证测的是线上代码);提取函数引用了脚本作用域变量时,沙箱里要补定义(如 `currentTab`、`renderBlocklog`)。
