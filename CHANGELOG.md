# Changelog

本项目所有重要变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 精神;版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)(x.y.z:主/功能/修复)。

## [0.14.3] - 2026-08-09

### Added
- **内置公开 gist 为默认远程数据源**:`remoteUrl` 默认指向公开 gist 稳定 URL(编辑 gist 内容不会改变 URL),自动同步开箱即用;私有数据源仍可自行覆盖。

### Changed
- 隐私红线细化(README / AGENTS.md):默认内置**仅允许公开安全的稳定数据源**;私有/secret gist URL 严禁写入脚本(URL 即访问钥匙)。

## [0.14.2] - 2026-08-09

### Added
- 屏蔽记录卡片新增独立 **[导出] 按钮**,下载 `blocklog.json`(独立备份,便于查看/交接)。

### Fixed
- 数据页打开时屏蔽记录不再"扫完回来不更新":屏蔽发生时若数据页处于打开状态,列表实时刷新。

## [0.14.1] - 2026-08-09

### Fixed
- **扫描通知条淡出后仍拦截点击**:`#xcbb-notify` 隐藏态补 `pointer-events:none`,修复扫描后面板 ⚙/🗕 按钮偶发不可点击(隐形覆盖层吃掉了点击)。

### Changed
- 全高模式(设置页)下通知条下移到标题栏下方,显示期间也不遮挡标题栏按钮。

## [0.14.0] - 2026-08-09

### Added
- **近期屏蔽记录(blocklog)**:每次屏蔽成功写入 `{h 手柄, n 昵称, t 时间, r 命中原因}`,按 handle 去重、上限 300 条;
- 数据 tab 新增"近期屏蔽记录"卡片:🏠 打开对方主页手动解封 / ✓ 一键加入白名单(防重扫再锁)/ ✕ 删除 / 清空;
- blocklog 随 下载 JSON、Gist 同步、JSON 导入 自动携带(按 handle 合并去重,保留最新时间戳,时间升序);
- 解决历史遗留:脚本此前从不记录"屏蔽了谁",误伤无法追溯——现在屏蔽记录 + 白名单形成误伤处理闭环。

## [0.13.1] - 2026-08-09

### Added
- 禁用按钮(▶ 扫描中 / ⏹ 待机)改为类禁用实现:保留点击事件,悬停显示原因(title),点击给出 toast 提示,不再"点了没反应"。

## [0.13.0] - 2026-08-09

### Fixed
- **评论关键词误屏蔽(普通词误伤)**:评论关键词匹配从"复用用户名名单"改为**独立列表**(内置低风险词 63 条中排除 9 个日常常见词:一夜情/处女/处子/脱衣/女仆/选妃/赌博/网赌/刷单 + 自定义列表);用户名名单(含挖掘词如"高中"/"免费")只匹配用户名,不再匹配评论文本。

### Changed
- **护眼浅色纸感主题**:米白纸底 + 低饱和绿主色 + 柔和红/琥珀;全部颜色收敛为 29 个 CSS 变量,一处调色全局生效;通知条/进度环/徽标统一。

### Security
- gitignore 保护用户数据:新增 `spam-rules*.json`、`*.swp` 防误提交(语料/名单禁止入库)。

## [0.12.14] - 2026-08-09

### Fixed
- 扫描结束二次复核:疑似数据不完整(缺昵称/文本过短)的未命中评论 force 重估,解决"刷新后才能识别";
- 闲置判定改用页面高度(修复底部自动加载被误判停止)。

## [0.12.x] - 2026-08-09

- **0.12.13** 扫描开始弹"进行中"通知(3s);通知组件零重建复用。
- **0.12.12** 扫描 try/finally 异常兜底(防 running/进度态残留);processed 会话去重上限 3 万。
- **0.12.11** 修复扫描提前停止(滚动感知闲置);Show-more 等待全文渲染;进行中/完成通知 3s。
- **0.12.10** 菜单快速扫描保持当前面板状态(收起时圆点直扫,不强制展开)。
- **0.12.9** 扫描完成改为右上角轻量通知(非模态);toast 统一通知系统。
- **0.12.8** 修复 GM_registerMenuCommand grant 缺失;TM 菜单精简为两项(快速扫描/进入设置)。
- **0.12.7** 改用 TM 原生菜单;修复自动同步缺失;光晕动画;停靠下拉。
- **0.12.6** 圆点左键直接扫描;圆点进度环/计数徽标;完成结果对话框。
- **0.12.5** 圆点左键=开始扫描;空白右键菜单固定右上角。
- **0.12.4** 圆点左键清理带反馈;右键恢复主界面;页面空白右键快捷菜单。
- **0.12.3** 静默屏蔽;圆点右键快捷菜单(停靠位置);扫描边界修复;启动自动同步。
- **0.12.0** 三维匹配 + 五 Tab 设置页 + 扫描边界修复 + 自动同步 + 文档。

## 仓库文档与工程化(非版本变更)

- **AGENTS.md**:面向后续 AI agent 的开发上手指南(工作流/隐私红线/已知缺口/Pros & Cons);
- **tests/ 入仓**:评论关键词(39 断言)与屏蔽记录(17 断言)逻辑测试迁入仓库,`node tests/*.test.mjs` 直接运行;
- **GREASYFORK.md**:GreasyFork 发布文档(元数据/简介/长描述/更新日志/检查清单);
- **PROMO.md / TWEETS.md**:宣传物料文案(3 图区块、一句话简介三版)+ X 推文/推文串文案(主推文三版、8 条 thread、互动备选);
- **README 全面重写**:执行图示、功能详解与实现依据、数据导出专章;
- 数据源 URL 隐私处理:全历史清除 secret gist URL,默认值仅内置公开稳定数据源。
