# 🚀 X 评论机器人屏蔽器 — GreasyFork 发布文档

> 本文件用于向 [GreasyFork](https://greasyfork.org) 提交/更新脚本。包含:提交元数据、简介(@description)、长描述(可粘贴到脚本页面的 About 区)、更新日志与免责声明。

---

## 一、提交元数据

| 字段 | 值 |
|---|---|
| 脚本名称 | X 评论机器人屏蔽器 |
| 名称(英文) | X Comment Bot Blocker |
| 命名空间 namespace | `xcbb` |
| 版本 version | `0.14.2` |
| 许可 license | MIT |
| 匹配 match | `https://x.com/*`, `https://twitter.com/*` |
| 作者 | kikuxdev |
| 源码地址(可选) | https://github.com/kikuxdev/X-Comment-Bot-Blocker |
| 更新地址(已内置于脚本) | updateURL / downloadURL → 本仓库 raw |

> ⚠️ 提交时**代码区**粘贴 `x-comment-bot-blocker.user.js` 全文(其头部元数据已配置齐全,GreasyFork 会自动解析)。

## 二、简介(@description 字段,已内置于脚本头部)

```
选取"机器人模板评论"或"不合理用户名(昵称/@handle)",一键扫描当前推文评论区,文本相似/用户名/评论关键词任一命中即自动屏蔽对应账号。内置约炮引流类高频规则模板(一键加载)、高频特征词挖掘、屏蔽记录(误伤可追溯/可导出)、数据全量导出与 Gist 同步,支持相似度阈值、白名单、试运行(仅标记)模式。
```

## 三、长描述(粘贴到 GreasyFork 脚本页 → About/描述 区)

以下为 **HTML 版**(GreasyFork 描述支持 HTML;若编辑器为 Markdown 模式,可直接粘贴本文件的 Markdown 主体)。

```html
<h3>📌 这是什么</h3>
<p>在 x.com 推文评论区选取一条 bot 评论作为<b>模板</b>或抓取一个 bot <b>账号</b>,一键扫描全部评论,对<strong>用户名命中 / 文本相似 / 评论含关键词</strong>的账号自动屏蔽。纯本地运行,数据全量可导出。</p>

<h3>✨ 核心特性</h3>
<ul>
  <li><b>三维匹配</b>:用户名(昵称/@handle)· 模板相似度 · 评论关键词,任一命中即处理</li>
  <li><b>模板驱动</b>:点击任意 bot 评论即成为匹配样本,动态维护模板库</li>
  <li><b>高频词挖掘</b>:已确认账号语料自动提炼高频特征(词频条形图可视化)</li>
  <li><b>防误伤</b>:评论关键词独立列表(普通词不会误伤评论)、相似度阈值可调、仅标记(试运行)模式、白名单、逐个确认</li>
  <li><b>防风控</b>:屏蔽间隔可调(默认 1.2s)、轮数上限、闲置自动停止、屏蔽结果验证</li>
  <li><b>屏蔽记录</b>:每次屏蔽留痕(昵称/账号/时间/命中原因),误伤可追溯、可导出、可跳主页解封、可加白名单</li>
  <li><b>数据全量导出</b>:下载 JSON(模板/名单/评论关键词/屏蔽记录/语料全量)、独立 blocklog.json、文本导出、Gist 远程同步</li>
  <li><b>界面</b>:主界面轻量操作 + 五 Tab 设置页 + 收起为停靠圆点 + 护眼浅色主题</li>
</ul>

<h3>🔬 工作原理</h3>
<ul>
  <li><b>三维匹配矩阵</b>:① 用户名(包含/正则)② 模板相似度(≥阈值)③ 评论关键词(可选),任一命中即处理</li>
  <li><b>相似度算法</b>:归一化(去 URL/@/#/emoji/标点)→ 三信号取最高:bigram Jaccard · Levenshtein 比 · 包含匹配</li>
  <li><b>屏蔽实现</b>:模拟真人点击路径(… 菜单 → Block → 确认 → 验证推文移除),不调用内部 API</li>
  <li><b>数据分层</b>:内置 91 条公开规则(随脚本发布)+ 私有 Gist(可选主数据源)+ 本地存储(运行时合并)</li>
</ul>

<h3>📖 快速上手</h3>
<ol>
  <li>安装 Tampermonkey 后导入脚本,打开 x.com 刷新,右上角出现悬浮面板</li>
  <li>点击 [＋ 选取评论],点击任意 bot 评论成为模板(可多选);或 [＋ 选取用户] 抓取 bot 账号</li>
  <li>点击 [▶ 扫描];建议先取消勾选"自动"跑一轮<b>仅标记</b>,确认命中质量后再开启自动屏蔽</li>
  <li>误伤处理:⚙ 数据 → 近期屏蔽记录 → 🏠 跳主页解封 + ✓ 加白名单</li>
</ol>

<h3>💾 数据导出</h3>
<ul>
  <li><b>下载 JSON(全量)</b>:⚙ 数据 → [下载 JSON],模板/名单/评论关键词/屏蔽记录/语料一个文件全带走</li>
  <li><b>导出 blocklog.json</b>:屏蔽记录卡片上的 [导出]</li>
  <li><b>Gist 同步</b>:填入私有 Gist raw URL → [📡 拉取并合并],跨设备恢复(合并去重,不覆盖)</li>
  <li>文本导出:导出全部 / 导出模板 / 导出日志(可发给作者生成补充规则)</li>
</ul>

<h3>⚠️ 注意事项</h3>
<ul>
  <li>自动屏蔽可能违反 X 服务条款,账号风险(限制/封禁)由使用者自行承担,仅供个人研究使用</li>
  <li>建议屏蔽间隔 ≥1000ms、每天 ≤100 个防风控</li>
  <li>名单中的脏数据(个人语料)建议留在本地或 secret Gist,勿上传公开仓库</li>
</ul>
```

若描述区为 **Markdown 模式**,可粘贴以下简版:

```markdown
选取 bot 评论作模板或抓取 bot 账号,一键扫描 X 评论区,对**用户名命中 / 文本相似 / 评论含关键词**的账号自动屏蔽。

- **三维匹配**:用户名 · 模板相似度 · 评论关键词,任一命中即处理
- **模板驱动** + **高频特征词挖掘**(词频条形图,一键加入名单)
- **防误伤**:评论关键词独立列表、阈值可调、仅标记试运行、白名单、逐个确认
- **防风控**:屏蔽间隔可调、轮数上限、屏蔽结果验证
- **屏蔽记录**:每次屏蔽留痕,误伤可追溯/可导出/可跳主页解封/可加白名单
- **数据全量导出**:下载 JSON(全量)、blocklog.json、文本导出、Gist 远程同步
- 内置 91 条高频规则一键加载;纯本地运行;护眼浅色主题

⚠️ 仅供个人研究,自动屏蔽可能违反 X 服务条款,风险自负。
```

## 四、更新日志(可贴在 About 底部)

```
v0.14.2  屏蔽记录卡片独立导出(blocklog.json)+ 数据页实时刷新
v0.14.1  修复扫描通知条淡出后拦截点击(⚙/🗕 按钮失效)
v0.14.0  新增近期屏蔽记录:误伤可追溯、可导出、可同步、白名单防重锁
v0.13.1  禁用按钮(▶/⏹)悬停与点击提示不可用原因
v0.13.0  评论关键词独立列表(修复普通词误屏蔽)+ 护眼浅色主题
```

## 五、提交检查清单

- [ ] 代码区粘贴最新 `x-comment-bot-blocker.user.js`(头部元数据完整:version 0.14.2 / license MIT / namespace xcbb)
- [ ] 简介使用第二节文本(与脚本 @description 一致)
- [ ] About 区粘贴第三节 HTML 长描述
- [ ] 更新日志粘贴到 About 底部(可选)
- [ ] 若脚本有截图,建议在 About 区补充 1-3 张界面截图(主面板 / 设置-数据 / 屏蔽记录)
